# Repository editing instructions

- Preserve Windows CRLF line endings in all C, C++, header, HLSL, and Markdown files.
- After modifying any of those files, normalize the complete modified file to CRLF.
- Before finishing, verify that modified source files contain no bare LF or bare CR line endings.
